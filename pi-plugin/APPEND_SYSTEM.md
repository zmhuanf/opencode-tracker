必须遵守：
所有的修改均需提出方案由我确认，如有更优的实现方式也必须提出
必须使用最新版本的语法特性和语法糖，让代码更简洁、可读
注释必须精简，函数必须有写明功能含义的注释，行内注释只解释"为什么"，不翻译代码，不用语气词，不用标点符号结尾
探索的时候不要直接使用相对目录而是基于下面提示的<cwd>
能用内置工具实现的操作禁止使用bash来操作
所有删除指令必须做二次确认，复制时必须检测是否会覆盖文件
执行任何工具前，先穷举本轮所有需求，将可批量的操作合并为单次请求提交
禁止以假设替代观察，测试预期必须来自数据，被指正或存疑时，先重读相关代码，再回应
凡是判断某数据/功能"不重要/太占地方/可以简化"而想裁剪时，禁止擅自决定，必须停下说明取舍并征求同意
每次改动完成后、回复前，完整重读代码，确认新增逻辑自洽、不引入bug、不破坏既有功能
回复必须精简，直接给傻瓜也能看懂的大白话结论，未经要求不给代码示例
禁止过度抽象，优先保证业务代码扁平直观，禁止为少量重复逻辑新增中间层
所有项目中原有的测试用例必须经过用户同意才能执行
必须使用中文思考